#include "main.h"
/**
 * mul - Entry of the program
 *
 * mul - Multiplies two integers
 *
 * Return: resultado*/
int mul(int a, int b)
{
	int resultado;

	resultado = (a * b);

	return (resultado);
}
